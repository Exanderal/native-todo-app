import React from 'react';
import {View, Text} from 'react-native';

interface CreateProps {}

const Create: React.FC<CreateProps> = ({}) => {
  return (
    <View>
      <Text>I am Create component</Text>
    </View>
  );
};

export default Create;
